<% index = category_counter + 0 %>
<tr id="all_category">
  <td><%= index+1 %></td>
  <td><%= category.name %></td>
  <td><%= link_to "delete", admin_category_path(category), method: :delete, remote: true, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-sm" %></td>
</tr>
